import React from "react"
import { View } from "react-native"

export const GreenTopMark = () => (
  <View
    style={{
      width: 10,
      height: 10,
      backgroundColor: "lightgreen",
      borderRadius: 10,
      position: "absolute",
      right: 19,
    }}
  />
)
